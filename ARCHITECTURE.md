# 架构说明

## 设计目标

机器人首先是自然对话 AI，其次才是规则和工具。规则只处理必须确定的边界，例如访问控制、保密、工具触发和小游戏状态；开放式聊天交给模型结合上下文完成。

## 分层

1. **协议层**：接收和发送 OneBot 11 事件，不绑定具体 QQ 实现。
2. **访问层**：只允许配置群和测试私聊账号，按会话加锁，防止并发回复乱序。
3. **缓存层**：SQLite WAL 单文件保存近 24 小时消息，以 QQ 号维护最新群名片索引。
4. **确定性能力层**：处理自我介绍、公开运行参数、功能说明和小游戏，不依赖模型可用性。
5. **检索层**：仅天津大学事实问题访问北洋维基；使用精准 GET 搜索、相关度排序、短超时和原文提取。
6. **对话层**：向模型提供分层上下文、当前日期、临时检索资料和保密策略。
7. **提供商层**：先调用 V4 Flash，失败后切换 V4 Pro；对 HTTP 错误、网络错误、空内容和异常 JSON 分别处理。
8. **降级层**：学校问题从已检索资料生成带来源的摘录；公开能力直接回答；其他问题使用不泄露内部故障的简短提示。

## 故障隔离

- OneBot 断开：主循环等待并重连。
- 连续消息：同一会话串行处理，直接消息不套群聊冷却。
- 维基不可用：继续尝试模型，但禁止猜测校内事实。
- 主模型不可用：切换备用模型。
- 两个模型均不可用：不输出“模型没有正常返回”等内部错误；优先返回本地事实或维基摘录。
- 单条消息处理异常：记录异常类型，并尽力向直接提问者发送安全降级回复。

## 上下文

每个群或私聊独立保存上下文，默认最多向模型提供 40 条详细消息，并附加近 24 小时人员索引。L1 是最近原文，L2 根据当前问题检索 24 小时内相关消息，L3 汇总每位用户的消息数。SQLite 缓存可跨重启，但不会读取缓存建立前的 QQ 历史。

每天 23:00 从同一缓存按 QQ 号统计近 24 小时发言数。姓名显示来自 `users` 索引；收到消息或 OneBot `group_card` 事件时更新，因此旧消息和汇总都会显示最新群名片。

## 安全

敏感值只从环境变量读取。公开仓库忽略 `.env`、协议账号配置、日志、缓存和运行状态。模型只收到“该请求属于秘密”的策略，不会收到被保护的实际值。
